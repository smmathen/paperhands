FINNHUB_API_KEY=

# Optional: Neon Postgres (hosted / multi-user). Omit to use local SQLite.
# DATABASE_URL=postgresql://...

# Portfolio scope per user. Use different ids to test multi-user locally.
DEV_USER_ID=local-dev
